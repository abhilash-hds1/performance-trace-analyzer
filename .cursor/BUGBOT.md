# Bugbot hints

- Flag any new secret or API key referenced from `extension/` (must stay server-side only).
- Flag missing updates to `docs/CHANGELOG.md` when `manifest.json` permissions change.
- Do not nitpick intentional `TODO(hackathon)` comments unless they affect security.
